# 📦 МастерСклад (MasterSklad)

WPF-приложение для автоматизации управления складскими помещениями и договорами аренды.

## 🚀 Инструкция по установке

Для корректной работы приложения выполните следующие шаги:

### 1. Подготовка базы данных
1. Установите **MS SQL Server** и **SSMS**.
2. Скачайте файл `sql_query.sql` из раздела [Releases](https://github.com/zumazoy-college/EP-0201-MW/releases/tag/v0.1).
3. Откройте его в SSMS и выполните **(F5)**. Это создаст базу данных `MasterSklad` и заполнит её начальными данными.

### 2. Установка приложения
1. Скачайте файл `MW_setup.exe` из раздела [Releases](https://github.com/zumazoy-college/EP-0201-MW/releases/tag/v0.1).
2. Запустите установщик и следуйте инструкциям.
3. По завершении запустите приложение через ярлык на рабочем столе.

---

## ⚙️ Настройка подключения

При первом запуске (или при изменении сервера БД):
1. В окне авторизации нажмите на иконку **шестеренки (⚙️)** в верхнем правом углу.
2. Введите строку подключения к вашему экземпляру SQL Server (лучше заменить только имя сервера **SQLEXPRESS**).
   * *Пример:* `Server=.\SQLEXPRESS;Database=MasterSklad;Trusted_Connection=True;TrustServerCertificate=True;`
3. Нажмите **"Проверить и сохранить"**. Если всё введено верно, появится сообщение об успешном подключении.

---

## 🔑 Доступ в систему

Для проверки функционала используйте предустановленные учетные записи:

| Роль | Логин | Пароль |
| :--- | :--- | :--- |
| **Администратор** | `admin` | `qwerty` |
| **Менеджер** | `manager` | `123456` |
| **Директор** | `director` | `123qwe` |

---
*Разработано в рамках EP-0201. Технологический стек: C#, WPF, EF Core, QuestPDF.*
