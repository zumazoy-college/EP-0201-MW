<Page x:Class="EP_0201_MW.Views.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EP_0201_MW"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Page.Resources>
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="Border" Background="White" BorderBrush="#D7DBDD" BorderThickness="1" CornerRadius="5"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="Placeholder" Text="Поиск по номеру склада..." 
                                       Foreground="Gray" Margin="10,0" VerticalAlignment="Center" IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder" Background="White" BorderBrush="#D7DBDD" BorderThickness="1" CornerRadius="5"/>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,30,0" VerticalAlignment="Center"/>

                            <ToggleButton Name="ToggleButton" Background="Transparent" BorderThickness="0" 
                                          ClickMode="Press" Focusable="false" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid Background="Transparent">
                                            <Path x:Name="Arrow" Fill="Gray" HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                  Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#D7DBDD" CornerRadius="5"/>
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button x:Name="BtnAdd" Content="➕ Добавить склад" Width="150" Height="35" 
                    Background="#27AE60" Foreground="White" 
                    Style="{StaticResource RoundedButtonStyle}" Click="BtnAdd_Click"/>

            <Button x:Name="BtnDelete" Content="🗑️ Удалить" Width="100" Height="35" Margin="10,0" 
                    Background="#E74C3C" Foreground="White" 
                    Style="{StaticResource RoundedButtonStyle}" Click="BtnDelete_Click"/>

            <TextBox x:Name="TxtSearch" Width="200" Height="35" 
                     Style="{StaticResource SearchTextBoxStyle}"
                     TextChanged="TxtSearch_TextChanged"/>

            <ComboBox x:Name="ComboStatus" Width="180" Height="35" Margin="10,0,0,0"
                      Style="{StaticResource RoundedComboBoxStyle}"
                      DisplayMemberPath="Title" 
                      SelectionChanged="ComboStatus_SelectionChanged"/>
        </StackPanel>

        <ListView x:Name="LViewWarehouses" Grid.Row="1" 
          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
          Background="Transparent" BorderThickness="0"
          MouseDoubleClick="LViewWarehouses_MouseDoubleClick">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Уменьшили ширину с 220 до 190 -->
                    <Border Width="190" Height="160" Background="White" CornerRadius="8" Margin="5">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>

                        <Border Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <!-- Номер склада -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- Адрес -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- Стоимость -->
                                    <RowDefinition Height="*"/>
                                    <!-- Пустое пространство -->
                                    <RowDefinition Height="Auto"/>
                                    <!-- Площадь и статус -->
                                </Grid.RowDefinitions>

                                <!-- Номер склада -->
                                <TextBlock Text="{Binding WarehouseNumber}" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="#2C3E50"/>

                                <!-- Адрес (сокращаем если слишком длинный) -->
                                <TextBlock Grid.Row="1" 
                                   Text="{Binding Object.Address}" 
                                   FontSize="10" 
                                   Foreground="#7F8C8D" 
                                   TextWrapping="Wrap" 
                                   Margin="0,4,0,0"
                                   MaxHeight="30"
                                   TextTrimming="CharacterEllipsis"/>

                                <!-- Стоимость аренды (добавили) -->
                                <Border Grid.Row="2" 
                                Background="#E8F4FD" 
                                CornerRadius="4" 
                                Padding="6,3" 
                                Margin="0,15,0,0">
                                    <TextBlock Text="{Binding MonthlyPrice, StringFormat={}{0:N0} ₽/мес}" 
                                       FontSize="18" 
                                       FontWeight="SemiBold" 
                                       Foreground="#2980B9"
                                       HorizontalAlignment="Center"/>
                                </Border>

                                <!-- Площадь и статус -->
                                <Grid Grid.Row="4" Margin="0,10,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Area, StringFormat={}{0} м²}" 
                                           VerticalAlignment="Center" 
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="#34495E"/>
                                    </StackPanel>

                                    <Border HorizontalAlignment="Right" 
                                    Padding="6,2" 
                                    CornerRadius="3" 
                                    Background="{Binding StatusId, Converter={StaticResource StatusToColorConverter}}">
                                        <TextBlock Text="{Binding Status.Title}" 
                                           Foreground="White" 
                                           FontSize="9"
                                           FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>